import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const DeliveryDetail = ({date}) => {
  return (
    <View>
      <Text>{date}</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default DeliveryDetail;
